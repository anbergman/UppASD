name: Bash with msys2

on:
  #release
  push:
  #pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  build_wheels:
    name: Build wheel for cp${{ matrix.python }}-${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash

    strategy:
      fail-fast: false
      matrix:
        include:
          # 64-bit Windows
          - os: windows-2019
            python: 37
            platform: win_amd64
          #- os: windows-2019
          #  python: 38
          #  platform: win_amd64
          #- os: windows-2019
          #  python: 39
          #  platform: win_amd64
          #- os: windows-2019
          #  python: 310
          #  platform: win_amd64
          #- os: windows-2019
          #  python: 311
          #  platform: win_amd64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      #- name: Install rtools 
      #  if: ${{ runner.os == 'Windows' }}
      #  run: |
      #    choco install -y rtools
      #    choco install -y pkgconfiglite

      - name: Setup MSYS
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          path-type: inherit
          pacboy: >-
            toolchain:p
            gcc-fortran:p
            openblas:p
            lapack:p
            fftw:p
            cmake:p
            ninja:p

      - name: Test environments before python
        env:
          MSYSTEM: MINGW64
        run: |
          echo 'which gfortran'
          which gfortran
          echo 'which cmake'
          which cmake
          echo 'which python'
          which python
          export PATH=/c/msys64/mingw64/bin:$PATH
          echo 'which gfortran'
          which gfortran
          echo 'which cmake'
          which cmake
          echo 'which python'
          which python
          export CMAKE_PREFIX_PATH=/mingw64
          export FC=gfortran
          export CC=gcc
          echo '=============== ls /'
          ls /
          echo '=============== ls /c/'
          ls /c/
          echo '=============== ls /c/msys64'
          ls /c/msys64
          echo '=============== ls /c/msys64/mingw64'
          ls /c/msys64/mingw64
          echo '=============== ls /c/msys64/mingw64/bin'
          ls /c/msys64/mingw64/bin
          echo '=============== ls /mingw64'
          ls /mingw64
          echo 'ls /mingw64/bin'
          ls /mingw64/bin
          echo '=============== tree /c/msys64'
          tree /c/msys64
          echo '=================================='
          env
          echo 'Find gfortran in msys'
          find /c/msys64 -name "*gfortran*"
          echo 'Find blas in msys'
          find /c/msys64 -name "*blas*"
          #echo 'Find gfortran in mingw'
          #find /mingw64 -name "*gfortran*"
          #echo 'Find blas in mingw'
          #find /mingw64 -name "*blas*"
          #cmake -S . -B build -DCMAKE_PREFIX_PATH=/mingw64
          #cmake --build build
          

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.9


      - name: Test environments after python
        run: |
          echo 'which python'
          which python
          echo 'which gfortran'
          which gfortran
          echo 'which cmake'
          which cmake
          env
      #- name: Test cmake with python
      #  env: 
      #    FC: gfortran
      #  run: |
      #    cmake -S . -B build -DBUILD_PYTHON=ON
      #    cmake --build build
      #    
      #- name: Build and test wheels
      #  uses: pypa/cibuildwheel@v2.14.0
      #  env:
      #    CIBW_BUILD: cp${{ matrix.python }}-${{ matrix.platform }}

      - name: Store artifacts
        uses: actions/upload-artifact@v3
        with:
          path: wheelhouse/*.whl

  ### build_sdist:
  ###   name: Build source distribution
  ###   runs-on: ubuntu-latest

  ###   steps:
  ###     - name: Checkout repository
  ###       uses: actions/checkout@v3

  ###     - name: Setup Python
  ###       uses: actions/setup-python@v4
  ###       with:
  ###         python-version: 3.9

  ###     - name: Build source distribution
  ###       run: bash python/build_tools/build_sdist.sh

  ###     - name: Test source distribution
  ###       run: bash python/build_tools/test_sdist.sh

  ###     - name: Store artifacts
  ###       uses: actions/upload-artifact@v3
  ###       with:
  ###         path: dist/*.tar.gz

